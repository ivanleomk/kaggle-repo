import { defineConfig } from 'unocss'

export default defineConfig({
  theme: {
    colors: {
      google: {
        blue: '#4285F4',
        red: '#EA4335',
        yellow: '#FBBC05',
        green: '#34A853',
      }
    }
  }
})
